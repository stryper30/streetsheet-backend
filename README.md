# StreetSheet Backend

Python/Flask backend for filling Carmel Street Department timesheets.

## Endpoints
- GET /health — health check
- POST /fill-timesheet — accepts template + data, returns filled xlsx
